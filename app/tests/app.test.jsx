var expect = require("expect");

//'it' describes the test we run and what it SHOULD do
//describe helps format the multiple tests better
describe('App', ()=>{
    it('should properly run tests', () => {
        expect(1).toBe(1);
    });
})